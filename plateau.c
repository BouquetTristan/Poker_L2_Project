#include "plateau.h"



/*
plateau_t * plateau_creer(void) {
	int i;
	plateau_t * plateau = malloc(sizeof(plateau_t));
	plateau->pot = 0;
	for(i = 0; i < NB_PLAYER; i++)
		plateau->player[i] = joueur_creer("toto");
	return(plateau);
}
*/

/*
void plateau_detruire(plateau_t ** plateau) {
	int i;
	for(i = 0; i < N; i++)
		carte_detruire( &((*jeu)->liste[i]) );
	free(*jeu);
	*jeu = NULL;
}
*/